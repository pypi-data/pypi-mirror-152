from setuptools import setup

setup(name = 'mhexport',
      version = '0.4',
      description = 'Mass Hunter Raw Data Export to Dataframe',
      packages = ['mhexport'],
      zip_safe = False)
