"""
High-Level-Interface (HLI) for the NormFlow package.
"""

# import torch
#
# import normflow as nf
#
#
# class HLIFlow(nf.flows.Flow):
#     def __init__(self, in_features, hidden_features, out_features, layers, shape):
#         transform = nf.transforms.CompositeTransform(
#
#         )
#
#
#         super().__init__(
#             transform=transform,
#             distribution=torch.distributions.Normal(
#                 loc=torch.zeros(shape),
#                 scale=torch.ones(shape),
#             )
#         )

