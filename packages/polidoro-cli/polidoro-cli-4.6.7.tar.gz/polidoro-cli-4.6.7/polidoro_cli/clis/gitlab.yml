name: GitLab

alias: glab

commands:
  cm:
    command:
        - cli git cp
        - echo "Waiting 5s..."
        - sleep 5
        - cli gitlab monitor
    help: Create the commit, push and monitor pipeline
    include_default_command: false
